# 🏗️ Arquitetura Visual - Sistema de Matching Inteligente

## Fluxo Completo do Sistema

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                       SISTEMA DE MATCHING INTELIGENTE                           │
│                         Smart Opportunity Matching v1.0                         │
└─────────────────────────────────────────────────────────────────────────────────┘


CAMADA 1: INTERFACE DO USUÁRIO
═══════════════════════════════════════════════════════════════════════════════════

┌──────────────────────────┐         ┌──────────────────────────┐
│    /dashboard            │         │ /dashboard/preferencias  │
│  (Dashboard Principal)   │         │  (Configurar Prefis)     │
├──────────────────────────┤         ├──────────────────────────┤
│ ✨ Recomendações Top 5   │         │ 🎛️  Form de Preferências │
│ 📊 Gráficos             │         │ 🔔  Canais Notificação   │
│ 📈 Métricas             │         │ 📋  Categorias Interesse │
│ 💳 Cards de Opp.        │         │ 💰  Faixa de Preço      │
│                          │         │ 📍  Localizações        │
└──────────────────────────┘         └──────────────────────────┘
         ▲                                    │
         │                                    │ (save preferences)
         │ (display matches)                  │
         │                                    ▼
         └─────────────────┬──────────────────┴──────────┐
                           │                             │
                    CAMADA 2: REACT COMPONENTS           │
                    ═══════════════════════════════════════════════════════════
                           │                             │
                   ┌────────▼────────┐          ┌────────▼────────┐
                   │RecommendedOpp.  │          │UserPreferencesF │
                   │recommended-opp..tsx         │user-prefs..tsx  │
                   ├────────┬────────┤          ├────────┬────────┤
                   │• Load top 5     │          │• Multi-select   │
                   │• Show score     │          │• Save to DB     │
                   │• Show reasons   │          │• Toast feedback │
                   │• Track clicks   │          │• Form validation│
                   └────────┬────────┘          └────────┬────────┘
                            │                           │
                            └─────────────┬─────────────┘
                                          │
                              CAMADA 3: SERVER ACTIONS & QUERIES
                              ═══════════════════════════════════════════
                                          │
                 ┌────────────────────────┼────────────────────────┐
                 │                        │                        │
          ┌──────▼──────┐         ┌──────▼──────┐         ┌──────▼──────┐
          │markMatchAs  │         │track        │         │fetch matches│
          │Clicked()    │         │Interaction()│         │from DB      │
          ├─────────────┤         ├─────────────┤         ├─────────────┤
          │• Update DB  │         │• User view  │         │• Server-side│
          │• Fire-forget│         │• User click │         │• Join tables│
          └─────────────┘         │• Analytics  │         └─────────────┘
                                  └─────────────┘


CAMADA 4: BANCO DE DADOS (Supabase PostgreSQL)
═══════════════════════════════════════════════════════════════════════════════════

┌─ user_preferences ──────────────┐
│ user_id (PK)                    │
│ interests: text[]               │
│ min_price, max_price            │
│ preferred_locations: text[]     │
│ urgency_level                   │
│ notification_frequency          │
│ email/push/sms enabled          │
└────────────────┬────────────────┘
                 │
                 │ (matches & recommends)
                 │
     ┌───────────▼───────────┐
     │  MATCHING HAPPENS     │
     │  HERE (see below)     │
     └───────────┬───────────┘
                 │
     ┌───────────▼────────────────────────────┐
     │ recommendation_matches (CORE TABLE)     │
     ├─────────────────────────────────────────┤
     │ id (PK)                                 │
     │ user_id (FK) + product_id (FK) = UNIQ  │
     │ match_score (0-100)                     │
     │ match_reasons: text[]                   │
     │ clicked / email_sent / email_opened     │
     │ created_at                              │
     └─────────────────────────────────────────┘

┌─ user_interactions ────────────┐
│ user_id, product_id (FKs)     │
│ interaction_type (viewed...)  │
│ duration_seconds              │
│ timestamp                     │
└───────────────────────────────┘

┌─ product_ai_scores ────────────┐
│ product_id (PK)               │
│ ai_score (cached)             │
│ ai_analysis, ai_reasons       │
│ cached_at (7 day expiry)      │
└───────────────────────────────┘

┌─ notifications ────────────────┐
│ user_id (FK)                  │
│ type (match_found, etc)       │
│ title, message                │
│ read / created_at             │
└───────────────────────────────┘


CAMADA 5: MATCHING ENGINE (Backend Logic)
═══════════════════════════════════════════════════════════════════════════════════

                         calculateHybridMatch()
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
            ┌──────────────┐ ┌─────────┐ ┌──────────────┐
            │ Rule-Based   │ │ Smart   │ │ Hybrid Score │
            │ (FAST)       │ │ Check   │ │ (BALANCED)   │
            ├──────────────┤ │         │ ├──────────────┤
            │ Categoria+35 │ │If score │ │ (Rules×0.6)+ │
            │ Preço   +30  │ │ ≥ 50    │ │ (AI×0.4)     │
            │ Local   +20  │ │   ↓     │ │              │
            │ Urgência+15  │ │ Call AI │ │ Result: 0-100│
            │              │ │         │ └──────────────┘
            │ MAX: 100 pts │ │         │         │
            └──────────────┘ │         │         │
                    │        │         │         │
                    └───────┬┴────┬────┴────┬────┘
                            │    │         │
                ┌───────────▼┐   │         │
                │ IF >= 65   │   │    ┌────▼──────────┐
                │   NOTIFY   │◄──┼────┤ (Final Score) │
                └────────────┘   │    └───────────────┘
                                 │
                        ┌────────▼─────────┐
                        │ OpenAI GPT-4 API │
                        │   (if needed)    │
                        ├──────────────────┤
                        │ Prompt: User+Prod│
                        │ Result: 0-100    │
                        │ Cache: 7 days    │
                        └──────────────────┘


CAMADA 6: NOTIFICAÇÕES & WEBHOOKS
═══════════════════════════════════════════════════════════════════════════════════

              sendMatchNotification() [Server Action]
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
   ┌─────────┐      ┌──────────┐    ┌──────────┐
   │ Resend  │      │ Supabase │    │ Webhook  │
   │ Email   │      │ Database │    │ Optional │
   ├─────────┤      ├──────────┤    ├──────────┤
   │ HTML    │      │ Insert   │    │ Discord/ │
   │ Template│      │ match    │    │ Slack    │
   │ 📧      │      │ Notify   │    │ 🔔       │
   │ TO: User│      │ Create   │    │ POST     │
   │         │      │ notif    │    │ Webhook  │
   └─────────┘      └──────────┘    └──────────┘
        │
        └──────────────────────────┬──────────────────────────┐
                                   │                          │
                          User Receives Email                 │
                          ✨ Nova Oportunidade!              │
                          Compatibilidade: 85%                │
                          [VER OPORTUNIDADE →]               │
                          Motivos: •Categoria                │
                                  •Preço ok                  │
                                  •Localização               │
                                   │                          │
                                   ▼                          │
                        Usuário Clica (Tracking)              │
                        └─────────────┬────────────────────────┘
                                      │
                                      ▼
                        trackInteraction('clicked')
                        Update: user_interactions
                        Update: clicked=true


CAMADA 7: CRON JOB (Execução Automática)
═══════════════════════════════════════════════════════════════════════════════════

              ⏰ Daily @ 8am UTC (Vercel Cron)
                        │
        GET /api/cron/matching
        Authorization: Bearer <CRON_SECRET>
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
    ┌────────────┐  ┌──────────┐  ┌──────────┐
    │ Fetch      │  │ Fetch    │  │ For Each │
    │ Users      │  │ Products │  │ User:    │
    ├────────────┤  ├──────────┤  ├──────────┤
    │ With notif │  │ Created  │  │ For Each │
    │ enabled    │  │ last 24h │  │ Product: │
    │ COUNT: N   │  │ COUNT: M │  │ MATCHES  │
    └────────────┘  └──────────┘  │ N × M    │
                                  └──────────┘
                                      │
                                      ▼
                        ┌─────────────────────────┐
                        │ calculateHybridMatch()  │
                        │ For each combination    │
                        │ IF score >= 65:         │
                        │   sendMatchNotif()      │
                        └────────┬────────────────┘
                                 │
                                 ▼
                         Return: {
                           success: true,
                           matches_found: X,
                           notifications_sent: Y
                         }


FLUXO COMPLETO DE DADOS
═══════════════════════════════════════════════════════════════════════════════════

USER CONFIGURATION FLOW:
┌──────────────────────────────────────────────────────────┐
│ User → /dashboard/preferencias → Form fills             │
│                                                           │
│ Save Preferences                                         │
│         ↓                                                │
│ INSERT/UPDATE user_preferences (Supabase)               │
│         ↓                                                │
│ Ready for next cron job                                 │
└──────────────────────────────────────────────────────────┘

MATCHING FLOW:
┌──────────────────────────────────────────────────────────┐
│ 1. Cron Job @ 8am UTC                                  │
│ 2. SELECT users WHERE notifications_enabled = true      │
│ 3. SELECT products WHERE created_at > 24h ago          │
│ 4. For each (user, product):                           │
│    • calculateRuleBasedMatch() [1ms]                   │
│    • If score >= 50: calculateAIMatch() [2-3s + cache] │
│    • calculateHybridScore()                            │
│    • If final >= 65:                                   │
│      └─> sendMatchNotification()                       │
│          ├─ Email via Resend                           │
│          ├─ Insert recommendation_matches              │
│          ├─ Create notification                        │
│          └─ Trigger webhook                            │
│ 5. Return results to Vercel logs                       │
└──────────────────────────────────────────────────────────┘

INTERACTION FLOW:
┌──────────────────────────────────────────────────────────┐
│ User Interacts with Product                             │
│         ↓                                                │
│ trackInteraction(productId, type, duration)             │
│         ↓                                                │
│ INSERT user_interactions (Supabase)                     │
│         ↓                                                │
│ Data feeds into next matching cycle                     │
│         ↓                                                │
│ System learns & improves recommendations                │
└──────────────────────────────────────────────────────────┘


SEGURANÇA & VALIDAÇÃO
═══════════════════════════════════════════════════════════════════════════════════

✅ RLS Policies:
   • user_preferences: Usuário vê apenas seus dados
   • user_interactions: Usuário vê apenas suas interações
   • recommendation_matches: Usuário vê apenas seus matches
   • notifications: Usuário vê apenas suas notificações

✅ API Security:
   • Cron: Bearer token validation (CRON_SECRET)
   • Server Actions: Auth required (via supabase.auth.getUser())
   • Emails: Email verification (exists in profiles)

✅ Data Integrity:
   • UNIQUE constraint: (user_id, product_id) em matches
   • Foreign Keys: Cascade delete para limpeza
   • Check Constraints: Enums validados

✅ Environment Variables:
   • OPENAI_API_KEY: Protegido no Vercel
   • RESEND_API_KEY: Protegido no Vercel
   • CRON_SECRET: Validado em cada request


MONITORAMENTO
═══════════════════════════════════════════════════════════════════════════════════

📊 Key Metrics:
   • Matches found per day
   • Click-through rate (CTR)
   • Email open rate
   • AI cache hit rate
   • Cron job success rate

🔍 Observability:
   • Vercel Logs: GET /api/cron/matching logs
   • Supabase Logs: Function execution logs
   • Database: Query & RLS policy logs
   • Resend: Email delivery tracking

⚠️ Alerts:
   • Cron job fails 3x in a row
   • OpenAI API errors exceed threshold
   • Email send failures increase
   • Database query performance degrades


═══════════════════════════════════════════════════════════════════════════════════
                           STATUS: 🟢 READY FOR PRODUCTION
═══════════════════════════════════════════════════════════════════════════════════
```

---

## 🔗 Relações entre Componentes

```
┌─────────────────────────────────┐
│   RecommendedOpportunities      │
│ (Client Component)              │
└──────────────┬──────────────────┘
               │
               ├─→ Fetches: recommendation_matches (user_id = current)
               │            Join with products table
               │
               ├─→ On Click: markMatchAsClicked(userId, productId)
               │
               └─→ Displays: Top 5 with scores


┌─────────────────────────────────┐
│   UserPreferencesForm            │
│ (Client Component)              │
└──────────────┬──────────────────┘
               │
               └─→ On Save: Upsert user_preferences
                           (user_id = current)


┌─────────────────────────────────┐
│   Cron Job: /api/cron/matching  │
│ (Backend Process)               │
└──────────────┬──────────────────┘
               │
               ├─→ Reads: user_preferences (notifications_enabled)
               │
               ├─→ Reads: products (created_at > 24h)
               │
               ├─→ Calls: calculateHybridMatch()
               │
               ├─→ Calls: sendMatchNotification()
               │
               ├─→ Writes: recommendation_matches
               │
               ├─→ Writes: notifications
               │
               └─→ Sends: Emails via Resend


┌─────────────────────────────────┐
│   Dashboard Page                │
│ (/dashboard)                    │
└──────────────┬──────────────────┘
               │
               ├─→ Renders: RecommendedOpportunities component
               │
               └─→ Shows: Top 5 personalized matches
```

---

**Sistema pronto e totalmente arquitetado! 🚀**

